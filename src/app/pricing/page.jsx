import Pricing from "../components/Pricing";

export const metadata = {
    title: 'Affinity softwares pricing',
    description: 'Buy once own forever',
  }

const Home = () => {
    return(
       <Pricing />
    )
}
export default Home;